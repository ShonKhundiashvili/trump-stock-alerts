# trump-stock-alerts — Python 3.11+
requests>=2.31
python-dotenv>=1.0
pydantic>=2.5
spacy>=3.7
rapidfuzz>=3.6
pandas>=2.1
yfinance>=0.2.40
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0
pytest>=8.0

# Optional LLM providers (only needed if you set OPENAI_API_KEY / ANTHROPIC_API_KEY):
#   pip install openai
#   pip install anthropic

# spaCy English model (install after requirements):
#   python -m spacy download en_core_web_sm
